import { INITIAL_VIEWPORT_ZOOM } from "../../constants/initialViewport";

export const INITIAL_MAP_CONFIG = {
  zoom: INITIAL_VIEWPORT_ZOOM,
  maxZoom: 15,
  fullscreenControl: false,
  rotateControl: false,
};
